# AI Agent

I built this just to see how these agenst actually work. There is one simple calculator application inside this project where the agent can run, debug and write codes. Nothing fancy. 

All the functions that can be called by the agent are inside the `functions/` directory.

> OpenRouter API for the LLM API. 



